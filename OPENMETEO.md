# OpenMETEOに関する問いあわせ

**※ 以前「グリッドに気象を重ねる情報源」として聞いていたのは、Open-Meteo の Historical Weather API で使える再解析データの「ERA5」です。**

1. 過去にさかのぼれるか。
2. 過去の記録の時間間隔は?
3. 未来の予報値を出せるか(今は使わないが、いずれ使う予定)
4. APW APIに組みこむべきか、それともクライアント側で直接取得すればいいか。
5. 風は風向・風速形式か、それとも速度のベクトル表現（u/v）か。

**解像度の目安:** Open-Meteo（ERA5 約25km、ERA5-Land 約11km）は地理院タイルよりやや粗い。地理院 Level 12（1タイル ≒ 約8km 前後）ならほぼ同等〜やや粗い程度で扱いやすい。

---

## 回答

### 1. 過去にさかのぼれるか

**できる。** Historical Weather API（`archive-api.open-meteo.com`）で過去データを取得可能。

- **ERA5**: 1940年〜現在（空間解像度 0.25° ≒ 約25km、**1時間間隔**）
- **ERA5-Land**: 1950年〜現在（0.1° ≒ 約11km、**1時間間隔**）
- **ECMWF IFS**: 2017年〜現在（9km、**1時間間隔**、更新が速い）
- 直近数日なら Forecast API の `past_days` パラメータでも取得可能。

出典: [Historical Weather API](https://open-meteo.com/en/docs/historical-weather-api)

### 2. 過去の記録の時間間隔は?

**基本は1時間（Hourly）。** データセットにより以下。

| データセット | 時間解像度 |
|-------------|------------|
| ERA5, ERA5-Land, ECMWF IFS, CERRA | **Hourly（1時間）** |
| ERA5-Ensemble | 3-Hourly（3時間） |
| IFS Assimilation | 6-Hourly（6時間） |

APWのグリッドと揃えるなら Hourly のデータセットを選べばよい。

### 3. 未来の予報値を出せるか(今は使わないが、いずれ使う予定)

**出せる。** Forecast API（`api.open-meteo.com/v1/forecast`）で対応。

- デフォルト: 7日先まで
- 最大: **16日先**まで指定可能
- オプションで「過去○日」も取得可能（`past_days`）

いずれ予報を重ねる場合も同じAPIで扱える。

### 4. APW APIに組みこむべきか、それともクライアント側で直接取得すればいいか

**どちらも可能。** 運用方針で選ぶとよい。

| 方式 | メリット | デメリット |
|------|----------|------------|
| **APW APIに組みこむ** | キャッシュ・レート制限をAPW側で一元管理できる。クライアント実装が単純。気象と大気汚染を1リクエストで揃えられる。 | APWの負荷・運用が増える。Open-Meteoの仕様変更時にAPWの修正が必要。 |
| **クライアントで直接取得** | APIキー不要なのでダッシュボード等からそのまま呼べる。APWの変更が不要。 | グリッド全セル分など多地点・多時間を取るとリクエスト数が増える。クライアント側でキャッシュやレート考慮が必要。 |

**目安:**

- 気象を「グリッド全体」で同じ時刻・同じ範囲として扱い、APWのグリッドと完全に揃えたい → **APW APIに組みこむ**（バックエンドでOpen-Meteoを叩き、グリッド用に加工・キャッシュして返す）のが扱いやすい。
- まずは少数地点や「現在値だけ」で試す → **クライアント直接**で十分。無料・キー不要なので実装が簡単。

### 5. 風は風向・風速形式か、それとも速度のベクトル表現（u/v）か

**風向・風速形式。** ベクトル成分はAPIでは提供されていない。

- **提供変数:** `wind_speed_10m`（風速）, `wind_direction_10m`（風向・度。0–360、風の**吹いてくる**方向）
- **単位:** 風速は km/h（デフォルト）。m/s, mph, knots も指定可能。
- **u/v が欲しい場合:** 風向・風速から自前で変換する。  
  - 気象の慣習（風向 = 風が吹いてくる方向）なら、  
    `u = -speed × sin(direction°)`, `v = -speed × cos(direction°)`  
    （東向きが u 正、北向きが v 正とする場合）。  
  - ライブラリでは MetPy の `wind_components` などが使える。

出典: [Historical Weather API - Hourly](https://open-meteo.com/en/docs/historical-weather-api), 変数 `wind_speed_10m`, `wind_direction_10m`。

